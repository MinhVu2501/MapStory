# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/mapstory

# JWT Secret (use a secure random string in production)
JWT_SECRET=your-super-secret-jwt-key-here

# Google Maps API Key
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for production CORS)
FRONTEND_URL=https://your-domain.com

# Optional: Database connection details (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mapstory
DB_USER=your-username
DB_PASSWORD=your-password 